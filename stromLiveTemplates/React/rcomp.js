// Scaffold for a simple stateles component
const $COMPONENT$ = function(props) {
   // Optional object-deconstruction for conviniet use of properties
  //const {foo, bar, baz} = props;

  return (
    <div>
      // Some JSX or Component goes here
    </div>
  );
}
